# .agent.md — Projeto Hibit

## 1. Objetivo do agente

Este agente deve ajudar a criar e evoluir o site institucional da empresa **Hibit**, com foco em:

- **Backend** em **.NET 8**
- **Frontend** em **Blazor WebAssembly**
- **Banco de dados** em **MySQL**
- **Mensageria** com **RabbitMQ**
- **Deploy** na **King.host**
- **CI/CD** com **GitHub Actions**

O agente deve priorizar:

- arquitetura limpa
- segurança da informação
- LGPD
- testes automatizados
- manutenção fácil
- código legível e sustentável

---

## 2. Escopo do site

O site terá inicialmente estas páginas:

- **Home**
  - apresentação da empresa
  - destaques institucionais e comerciais

- **Sobre**
  - história da empresa
  - missão, visão, valores e diferenciais

- **Contato**
  - formulário de contato
  - mapa
  - envio de mensagem para contato@hibit.com.br
  - a mensagem não deve ser salva no banco
  - a mensagem deve ser criptografada antes de ser enviada ao RabbitMQ
  - o processamento posterior será feito por outra aplicação de controle

---

## 3. Regras de negócio

### 3.1 Formulário de contato

- Não persistir dados do formulário em banco de dados
- Validar todos os campos
- Exigir consentimento explícito da LGPD
- Enviar os dados via RabbitMQ
- Criptografar o payload antes da publicação na fila
- Não registrar dados sensíveis em logs

### 3.2 E-mail

- Destino fixo: contato@hibit.com.br

### 3.3 Mapa

- A página de contato deve exibir um mapa da empresa

---

## 4. Stack técnica

### Backend

- ASP.NET Core 8
- Web API
- Entity Framework Core
- MySQL
- RabbitMQ
- Serilog
- FluentValidation
- MediatR
- AutoMapper

### Frontend

- Blazor WebAssembly
- Reactive Forms
- Lazy loading
- Componentes organizados por responsabilidade

### Testes

- Testes unitários
- Testes de integração

---

## 5. Arquitetura desejada

O backend deve seguir uma arquitetura limpa com estas camadas:

- Domain
- Application
- Infrastructure
- API

### Regras da arquitetura

- Separar responsabilidades
- Evitar acoplamento desnecessário
- Manter regras de negócio fora da infraestrutura
- Isolar integrações externas
- Facilitar testes

---

## 6. Banco de dados

- Usar MySQL
- Usar Entity Framework Core
- Usar migrations
- **Aplicar migrations automaticamente no startup** (`DatabaseInitializer`): cria o database MySQL se não existir e aplica migrations pendentes
- Evitar SQL cru sempre que possível
- Não usar concatenação de strings em consultas
- Nunca expor credenciais no código
- O formulário de contato não deve ser salvo no banco
- O AppDbContext deve conter DbSet para as entidades do sistema
- Novas tabelas devem ser criadas de forma organizada e explícita
- Tabela `usuarios`: armazena o **usuário de aplicação** (credenciais para o frontend acessar a API via JWT)
- Senhas na tabela `usuarios` devem ser armazenadas apenas como hash (nunca em texto puro)

---

## 7. Segurança da informação

### Obrigatório

- Usar HTTPS
- Proteger contra SQL Injection
- Evitar XSS
- Evitar CSRF
- Validar entrada de dados no backend e no frontend
- Não hardcodar segredos
- Usar variáveis de ambiente para tudo que for sensível
- Não logar PII
- Aplicar o princípio do menor privilégio
- Proteger endpoints da API com **JWT Bearer**
- O frontend deve obter token via `POST /api/auth/login` e enviar `Authorization: Bearer {token}` nas requisições
- Armazenar o token no frontend em `sessionStorage` (não em `localStorage`)

### Recomendado

- Configurar Cloudflare para proteção adicional
- Usar headers de segurança HTTP
- Restringir CORS apenas ao frontend autorizado
- Tratar formulários com cuidado para evitar abuso e vazamento de dados

### LGPD

- Exibir aviso de privacidade
- Solicitar consentimento explícito no formulário
- Coletar apenas o necessário
- Não armazenar dados desnecessários
- Garantir tratamento seguro das mensagens

---

## 8. RabbitMQ

O fluxo do contato deve ser:

1. Usuário envia o formulário
2. Backend valida os dados
3. Backend criptografa o payload com AES-256
4. Backend publica a mensagem na fila RabbitMQ
5. Outra aplicação consome e processa a mensagem

### Regras

- Não persistir os dados do contato em banco
- Criptografar o payload antes do envio
- Ler chaves e segredos por variável de ambiente
- Separar claramente produtor e consumidor

### Produção (King.host)

| Item | Valor |
|------|-------|
| Host RabbitMQ | `rabbit.hibit.com.br` |
| Usuário | `admin` |
| Fila | `hibit.contact` |
| Senha | Configurar via `RabbitMQ__Password` no painel (nunca no repositório) |

---

## 9. Variáveis de ambiente

Sempre que houver valores que possam mudar entre ambientes, usar variáveis de ambiente.

### Exemplos

- string de conexão do banco
- host do RabbitMQ (`rabbit.hibit.com.br` em produção)
- usuário e senha do RabbitMQ
- chave de criptografia AES-256
- IV de criptografia
- origem permitida no CORS
- URLs públicas
- credenciais de SMTP
- tokens e segredos de deploy (SSH, JWT produção)
- `ASPNETCORE_PATHBASE` (produção: `/API`, app em `/API/` na raiz FTP)
- `Jwt__Secret`, `Jwt__Issuer`, `Jwt__Audience`, `Jwt__ExpirationMinutes`
- `AppUser__Username`, `AppUser__Password` (seed do usuário de aplicação)

### Regras

- Nunca hardcodar segredos **de produção**
- Nunca deixar senha de produção no repositório
- Sempre criar arquivo de exemplo chamado `.env.example`
- Credenciais de **Docker local** (desenvolvimento) ficam documentadas abaixo e em `.env.example`

### Docker local (desenvolvimento)

O Docker deste projeto roda **no WSL**, não no Windows. Executar `docker compose` no PowerShell sem integração WSL/Docker Desktop não sobe os containers.

| Serviço | Variável | Valor local |
|---------|----------|-------------|
| MySQL | `MYSQL_ROOT_PASSWORD` | `erika03` |
| MySQL | `MYSQL_DATABASE` | `hibit_dev` |
| MySQL | conexão da API | `User=root;Password=erika03;Database=hibit_dev` |
| RabbitMQ | `RABBITMQ_DEFAULT_USER` | `admin` |
| RabbitMQ | `RABBITMQ_DEFAULT_PASS` | `admin123` |
| RabbitMQ | fila | `hibit.contact` |

Comando para subir a infra local (**terminal WSL**, na raiz do repo):

```bash
cd /mnt/c/Users/vladi/source/repos/SiteHibit   # ajuste o caminho se necessário
docker compose -f docker/docker-compose.yml --env-file .env up -d
```

**Aplicações fora do Docker (desenvolvimento manual)**

| Aplicação | Ferramenta | Porta |
|-----------|------------|-------|
| Frontend Blazor WASM | Visual Studio / `dotnet run` em `frontend/hibit-blazor` | 5050 |
| Backend .NET | Visual Studio — projeto `Hibit.Api` | 5000 |

**Conectividade WSL ↔ Windows**

- Se backend e frontend rodam **no WSL**: use `localhost` para MySQL e RabbitMQ.
- Se backend roda **no Windows** e Docker **no WSL**: use o IP do WSL em `ConnectionStrings__DefaultConnection` e `RabbitMQ__Host` se `localhost` falhar.

Management UI do RabbitMQ: http://localhost:15672 (usuário `admin`).

### MySQL produção (King.host)

| Item | Valor |
|------|-------|
| Host | `mysql.hibit.com.br` |
| Database | `hibit` |
| User | `hibit` |
| Senha | Configurar **apenas no painel** da hospedagem (nunca no repositório) |

### JWT e usuário de aplicação (desenvolvimento)

| Item | Valor / configuração |
|------|----------------------|
| Login | `POST /api/auth/login` |
| Usuário seed | `hibit-app` (via `AppUser__Username`) |
| Senha seed | `hibit-app-2026` (via `AppUser__Password`) |
| JWT Secret | `Jwt__Secret` (mínimo 32 caracteres) |
| Frontend | sem credenciais no cliente; contato anônimo |

---

## 18. Autenticação JWT — usuário de aplicação

O site institucional **não exige login do visitante**. O formulário de contato é **anônimo** (chama `/api/contact` diretamente, protegido por rate limiting + validação). O fluxo JWT/AppUser abaixo está reservado para **uso administrativo futuro** e não é usado pelo site público.

### Fluxo (reservado para admin futuro)

1. Ao iniciar, o frontend chama `POST /api/auth/login` com usuário/senha de aplicação
2. A API valida credenciais na tabela `usuarios` e retorna `{ accessToken, expiresAt }`
3. O frontend guarda o token em `sessionStorage`
4. Um interceptor HTTP envia `Authorization: Bearer {token}` em todas as requisições `/api/*` (exceto login)
5. Endpoints protegidos (ex.: admin futuro) exigem token válido; o contato público é anônimo (sem JWT no cliente)

### Backend

- Entidade: `Usuario` → tabela `usuarios`
- Migration EF Core aplicada automaticamente no startup (`DatabaseInitializer`)
- Seed automático do usuário de aplicação a partir de `AppUser:Username` e `AppUser:Password` (reservado para uso admin futuro)
- Pacote: `Microsoft.AspNetCore.Authentication.JwtBearer`

### Frontend

- O contato é enviado diretamente a `/api/contact` (anônimo, sem credenciais no cliente)
- `ThemeService`: tema claro/escuro com persistência em `localStorage`
- Não há fluxo de login no WASM; o JWT/AppUser fica apenas na API

### Endpoints

| Método | Rota | Autenticação |
|--------|------|--------------|
| `POST` | `/api/auth/login` | Pública (reservado para admin futuro) |
| `GET` | `/health` | Pública |
| `POST` | `/api/contact` | Pública (anônima, com rate limiting) |

### Regras

- Não logar tokens nem senhas
- Em produção, usar `Jwt__Secret` forte via variável de ambiente
- Em produção, definir credenciais do usuário de aplicação apenas no servidor (seed via `web.config`); **não embarcar credenciais no frontend Blazor**
- O formulário de contato não exige autenticação; a proteção contra abuso é via rate limiting + validação

---

## 10. Testes

### Testes unitários

Criar testes para:

- validações
- casos de uso
- serviços de criptografia
- publicação na fila RabbitMQ
- regras de negócio
- login (`LoginValidator`) e geração/validação JWT

### Testes de integração

Criar testes para:

- endpoint de contato (com token JWT válido)
- `POST /api/auth/login` (credenciais do usuário de aplicação)
- requisição sem token retornando `401 Unauthorized`
- integração com banco
- integração com RabbitMQ quando aplicável
- comportamento dos endpoints principais

Implementados em `Hibit.Tests`:

- `Unit/LoginValidatorTests`
- `Unit/AesEncryptionServiceTests`, `Unit/SubmitContactValidatorTests`
- `Integration/ContactEndpointTests` (login, contato autenticado e sem token)

### Regras

- Testes devem acompanhar novas funcionalidades
- Preferir testes claros e fáceis de manter
- Cobrir casos de sucesso e falha

---

## 11. frontend Blazor WebAssembly

### Páginas

- Home
- Sobre
- Contato
- Privacidade

### Identidade visual

Paleta obrigatória: **branco**, **preto**, **cinza** e **verde** (acento).

| Token | Uso |
|-------|-----|
| `#0a0a0a` / `#141414` | Fundos escuros, footer, hero |
| `#ffffff` / `#fafafa` | Fundos claros, cards |
| `#71717a` – `#e4e4e7` | Texto secundário, bordas |
| `#22c55e` / `#16a34a` | CTAs, destaques, logo mark |

- Tipografia: **Inter** (Google Fonts)
- Tokens CSS em `frontend/hibit-blazor/wwwroot/styles/global.css`
- Conteúdo institucional centralizado em `Models/SiteContent.cs`
- Layout: header sticky com menu mobile, seções modulares (hero, stats, serviços, processo, CTA)
- Botão flutuante de WhatsApp (`Shared/WhatsAppFab.razor`): fixo no canto inferior direito em todas as páginas; abre `https://wa.me/5535920014611` em nova aba; número centralizado em `SiteContent.Company`
- Referências de design: sites minimalistas B&W com acento verde (agências de software / dev shops)

### Regras

- Usar componentes organizados
- Validar formulários no frontend
- Ter feedback claro para o usuário
- Garantir responsividade (mobile-first)
- Verificar erros de português brasileiro nas telas
- Evitar duplicação de código
- Manter consistência com a paleta de cores definida

### Boas práticas

- Separar serviços
- Usar tipagem forte
- Evitar lógica de negócio dentro dos componentes
- Carregar páginas sob demanda quando possível

### Autenticação (usuário de aplicação)

O frontend **não** expõe tela de login ao visitante e **não** embarca credenciais no cliente. O formulário de contato é anônimo (chama `/api/contact` diretamente, protegido por rate limiting + validação). O fluxo JWT/AppUser permanece na API reservado para uso administrativo futuro:

- `AuthController` — login em `POST /api/auth/login` (público, com rate limiting)
- Credenciais do usuário de aplicação configuradas no servidor via `web.config` (`AppUser__Username`, `AppUser__Password`)
- O Blazor WASM não realiza login nem guarda tokens

Detalhes completos na seção **18. Autenticação JWT**.

## 12. Deploy e publicação

### Branches

| Branch | Uso |
|--------|-----|
| `Development` | Desenvolvimento e PRs |
| `master` | Produção — merge dispara deploy |

Fluxo: PR **`Development` → `master`** (aprovado) → GitHub Actions publica na King.host.

Repositório: `git@github.com:vladimirca2000/SiteHibit.git`

### GitHub Actions

| Workflow | Disparo | Função |
|----------|---------|--------|
| `backend-ci.yml` | push/PR em `Development` ou `master` | build + testes backend |
| `frontend-ci.yml` | push/PR em `Development` ou `master` | publish Blazor WASM |
| `deploy-production.yml` | push em `master` | testes + build + deploy FTP `/www/` e `/www/API/` |

### King.host (produção)

| Caminho FTP | Conteúdo |
|-------------|----------|
| `/www/` | Blazor WASM (site estático) |
| `/www/API/` | ASP.NET Core 8 (API REST) |

- Frontend em produção usa `apiUrl: '/API'`
- API usa `ASPNETCORE_PATHBASE=/API` (URL `/API/`, pasta física `www/API`)
- Variáveis sensíveis no `web.config` de `/www/API/` (gerado no deploy)
- Deploy via **FTP**: `publish/api/` → `/www/API/` (clean-slate, pasta própria), depois `publish/www/` → `/www/` (sync sem apagar `/www/API/`); fallback FTPS na porta 990
- Secrets GitHub: `FTP_*`, `MYSQL_PASSWORD`, `RABBITMQ_PASSWORD`, `JWT_SECRET`, `ENCRYPTION_KEY`, `ENCRYPTION_IV`, `APP_USER_PASSWORD`

Scripts locais: `scripts/publish-frontend.*`, `scripts/publish-api.*`

---

## 13. Convenções de desenvolvimento

- Escrever código limpo
- Evitar duplicação
- Usar nomes claros para classes, métodos e variáveis
- Preferir soluções simples e sustentáveis
- Não criar complexidade desnecessária
- Antes de implementar algo novo, verificar impacto na arquitetura
- Sempre considerar segurança e testes

---

## 14. Como este agente deve trabalhar

### Comportamento geral

- Ler este arquivo antes de qualquer implementação
- Manter o contexto das decisões anteriores
- Registrar novas regras quando surgirem
- Evitar quebrar padrões já definidos
- Manter compatibilidade com o que já foi entregue

### Ao implementar funcionalidades

Antes de gerar qualquer código, verificar:

- Impacto no banco de dados
- Impacto na segurança
- Necessidade de nova variável de ambiente
- Necessidade de novo teste
- Impacto no deploy

### Ao gerar código

- Seguir SOLID
- Evitar acoplamento desnecessário
- Preferir código explícito e simples
- Evitar soluções difíceis de manter
- Usar validações consistentes
- Proteger endpoints sensíveis
- Não gerar queries vulneráveis a SQL Injection
- Não armazenar dados do formulário de contato

### No backend

- Usar serviços bem definidos
- Separar controller, serviço, repositório e integração externa
- Manter regras de negócio na camada correta

### No frontend

- Organizar componentes por feature
- Usar serviços para acesso à API
- Usar formulários reativos
- Mostrar mensagens claras de validação

---

## 15. Atualização deste arquivo

Sempre que uma nova funcionalidade for concluída:

- Adicionar entrada no Changelog
- Atualizar ou adicionar regras relevantes
- Registrar decisões de arquitetura tomadas

---

## 16. Estrutura esperada do repositório

- `hibit/`
  - `backend/`
    - `Hibit.Api/`
    - `Hibit.Application/`
    - `Hibit.Domain/`
    - `Hibit.Infrastructure/`
    - `Hibit.Tests/`
      - `Unit/`
      - `Integration/`
  - `frontend/`
    - `hibit-blazor/`
  - `.github/`
    - `workflows/`
      - `backend-ci.yml`
      - `frontend-ci.yml`
      - `deploy-production.yml`
  - `scripts/`
    - `publish-frontend.sh` / `.ps1`
    - `publish-api.sh` / `.ps1`
  - `.env.example`
  - `.agent.md`

---

## 17. Changelog

### v0.7.0 — Contato anônimo + pipeline FTP-only

- `/api/contact` agora é **anônimo** (removido `[Authorize]`); mantém rate limiting + validação
- Removido o fluxo de login do Blazor WASM (`AuthService`, `AuthMessageHandler`, `AppUser` no `appsettings` do cliente) — a senha de produção deixa de ficar exposta no navegador
- Backend: `UseForwardedHeaders` (evita loop de redirect atrás do IIS), exception handler movido para o início do pipeline com logging, rate limit no login, validação do Encryption no startup, resiliência do RabbitMQ (503 em vez de 500 opaco), dedup de `JwtOptions`
- Frontend: corrigido `ThemeService` (matchMedia via helper JS), CSS do `#blazor-error-ui`, erros do contato (400/429/503) exibidos ao usuário, removidos `appsettings` duplicados na raiz do projeto
- Pipeline `deploy-production.yml`: **FTP-only** (removido push das branches `release-www`/`release-api`); API primeiro (clean-slate em `/www/API/`) → frontend (sync em `/www/` sem apagar a API); fallback FTPS; workflow **falha** se FTP falhar; health checks com retries não-fatais; `test-frontend` alinhado com `publish-frontend.sh`; removida injeção de `APP_USER_PASSWORD` no build do frontend

### v0.6.0 — API em /www/API/ + web.config IIS

- Deploy FTP da API para `/www/API/` (mapeamento IIS da King.host)
- `scripts/web.config.production.template` + `generate-api-webconfig.*` injetam secrets no IIS
- Painel King.host: manter **Nenhuma versão** ASP.NET Framework; Core sobe via `web.config`

### v0.5.9 — Deploy FTP restaurado + web.config IIS

- `deploy-production.yml` volta a publicar via `SamKirkland/FTP-Deploy-Action` em `/www/` e `/API/`
- Secrets GitHub: `FTP_SERVER`, `FTP_USERNAME`, `FTP_PASSWORD`, `APP_USER_PASSWORD`
- `frontend/hibit-blazor/wwwroot/web.config`: rewrite SPA no IIS (inclui MIME `.wasm`), exclui `/API`
- API: `web.config` gerado automaticamente pelo `dotnet publish` dentro de `/API/` (não na raiz FTP)
- API exige cadastro ASP.NET Core no painel King.host (caminho `\API`) + variáveis de ambiente

### v0.5.8 — API fora de /www/ (padrão King.host)

- API volta para `/API/` na raiz FTP (irmã de `/www/`)
- Removido deploy em `/www/API/`; painel ASP.NET em `\API`

### v0.5.7 — API em `/www/API/` (dentro de www)

- Deploy da API para `/www/API/` no domínio principal
- Restaurados `apiUrl: '/API'` e `ASPNETCORE_PATHBASE=/API`

### v0.5.6 — API no subdomínio api.hibit.com.br

- Frontend prod aponta para `https://api.hibit.com.br`
- Removido `ASPNETCORE_PATHBASE=/API`; API na raiz do subdomínio
- Deploy API via Git Webhook em `api.hibit.com.br` → `/www/`

### v0.5.5 — Branch de desenvolvimento renomeada

- Branch `main` renomeada para `Development`
- CI e documentação atualizados

### v0.5.4 — Retorno ao Git Webhook King.host

- Pipeline volta a publicar branches `release-www` e `release-api`
- Removido deploy FTP; King.host sincroniza via Git Webhook no painel

### v0.5.5 — Release branches com histórico preservado

- `deploy-production.yml` passa a clonar `release-www` e `release-api` antes de publicar artefatos
- Removido `push -f` com branch órfã; releases agora seguem com histórico Git normal
- Ajuste feito para melhorar compatibilidade com a sincronização Git Webhook da King.host
- Workflow passa a gravar `.deploy-marker` para forçar novo commit e novo sync na King.host em todo deploy

### v0.5.3 — Deploy via FTP King.host

- Pipeline envia artefatos compilados por FTP (`/www/` + `/API/`)
- Secrets: `FTP_SERVER`, `FTP_USERNAME`, `FTP_PASSWORD`
- Removido push de branches `release-www` / `release-api`

### v0.5.2 — Deploy alinhado ao Git Webhook King.host

- Pipeline publica branches `release-www` e `release-api` (artefatos compilados)
- Removido deploy SSH; King.host sincroniza via Git Webhook no painel
- Documentação atualizada conforme wiki King.host

### v0.5.1 — RabbitMQ reintroduzido + config produção

- Restaurados `RabbitMqPublisher`, fila `hibit.contact` e criptografia AES no contato
- `appsettings.Production.json` com hosts MySQL/RabbitMQ de produção
- Docker local e CI com RabbitMQ novamente
- Variáveis de produção documentadas (senhas somente no painel King.host)

### v0.5.0 — Remoção RabbitMQ + CD King.host

- Removido RabbitMQ (código, Docker, CI, config)
- `NoOpContactPublisher` substitui fila (formulário retorna 202; próximo passo: SMTP)
- Deploy separado: `/www/` (Blazor WASM) e `/www/API/` (API .NET)
- Workflow `deploy-production.yml` — deploy automático no push em `master`
- CI atualizado para branches `main` e `master`
- Scripts `publish-frontend` e `publish-api`
- API com `PathBase` e CORS configurável; frontend prod com `apiUrl: '/API'`
- MySQL produção documentado (`mysql.hibit.com.br`)

### v0.4.2 — Botão flutuante WhatsApp

- Componente `WhatsappFabComponent` no layout principal (visível em Home, Sobre, Contato e Privacidade)
- Link externo para WhatsApp da Hibit (`+55 35 92001-4611`) via `https://wa.me/5535920014611`
- Dados do WhatsApp centralizados em `site-content.ts` (`company.whatsapp`)

### v0.4.0 — Redesign do frontend institucional

- Identidade visual profissional (branco, preto, cinza, verde)
- Home expandida: hero, estatísticas, serviços, processo, tecnologias e CTA
- Páginas Sobre, Contato e Privacidade redesenhadas
- Header com menu mobile; footer em múltiplas colunas
- Tipografia Inter; tokens CSS centralizados
- Conteúdo institucional ampliado em `Models/SiteContent.cs`

### v0.4.1 — Docker apenas infraestrutura

- Removido serviço `frontend` do Docker Compose (MySQL e RabbitMQ apenas)
- Frontend e backend rodam manualmente: VS Code (:5050) e Visual Studio (:5000)

### v0.4.0 — Redesign do frontend institucional

- Documentado que MySQL/RabbitMQ sobem via Docker **no WSL** (não no Windows)
- README e `.agent.md` com caminho WSL, ordem de subida e troubleshooting de conexão Windows → WSL

### v0.3.1 — Migrations automáticas no startup

- `DatabaseInitializer` cria o database MySQL se não existir e aplica migrations pendentes
- Seed do usuário de aplicação após migrations

### v0.3.0 — Autenticação JWT e usuário de aplicação

- Tabela `usuarios` com migration EF Core
- `POST /api/auth/login` retorna JWT
- Endpoints da API protegidos com `[Authorize]` (contato exige token)
- Seed do usuário de aplicação (`hibit-app`) via configuração `AppUser`
- Frontend: `AuthService`, interceptor HTTP e token em `sessionStorage`
- Swagger com suporte a Bearer token

### v0.2.1 — Credenciais Docker local

- MySQL local: `MYSQL_ROOT_PASSWORD=erika03`, database `hibit_dev`, conexão via `root`
- RabbitMQ local: `admin` / `admin123`
- Backend (`appsettings.json`, `appsettings.Development.json`), `.env.example` e `docker-compose.yml` alinhados

### v0.2.0 — Implementação inicial do site

- Fundação do repositório (.gitignore, .env.example, Docker Compose, README)
- Backend .NET 8 Clean Architecture com health check, contato via RabbitMQ e AES-256
- Frontend Blazor WASM (.NET 8) com rotas (Home, Sobre, Contato, Privacidade)
- Testes unitários e de integração do backend
- GitHub Actions (backend-ci.yml, frontend-ci.yml)
- Script de publish Blazor WASM para King.host

### v0.1.0 — Estrutura inicial

- Definição da stack técnica
- Definição das páginas do site
- Regras de segurança da informação
- Regras de LGPD
- Fluxo do RabbitMQ
- Regras para variáveis de ambiente
- Diretrizes para testes
- Diretrizes para deploy na King.host
- Estrutura do repositório
